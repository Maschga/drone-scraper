[project]
name = "drone-scraper"
version = "0.2.0"
description = "Hourly FPV drone price tracker for iFlight and FPV24"
requires-python = ">=3.12"

dependencies = [
    "apscheduler>=3.11,<4",
    "beautifulsoup4>=4.13,<5",
    "fastapi>=0.116,<1",
    "httpx>=0.28,<1",
    "tzdata>=2025.2",
    "uvicorn[standard]>=0.35,<1",
]
